{
  "name": "curso-git-scesi",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix fronted run dev",
    "dev:frontend": "npm --prefix fronted run dev",
    "dev:backend": "npm --prefix backend run start:dev",
    "build": "npm --prefix fronted run build",
    "lint": "npm --prefix fronted run lint"
  }
}
